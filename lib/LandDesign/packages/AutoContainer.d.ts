import { default as React } from 'react';
export type AutoContainerProps = {
    ratio?: number;
    wrapClassName?: string;
    children?: React.ReactNode;
};
declare const AutoContainer: React.FC<AutoContainerProps>;
export default AutoContainer;
